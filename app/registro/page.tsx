// /registro — alta del publicador (envivo-publisher).
//
// Punto de partida. Acá va el formulario de registro que hoy vive en
// envivo/app/registro (Google + verificación por SMS y correo vía Twilio
// Verify) — ver CLAUDE.md del repo principal, "Alta del publicador", para
// el flujo completo a migrar. Sin cobro: el registro es gratis.

export default function Registro() {
  return (
    <main>
      <h1>Registro de publicador</h1>
      <p>Formulario en construcción.</p>
    </main>
  );
}
